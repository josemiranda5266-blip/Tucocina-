# Tucocina — Auditoría técnica 2026-09-09

## Objetivo

Dejar el MVP preparado para crecer con **mínimo costo operativo**, buena seguridad y consultas de Firestore eficientes. La prioridad es evitar descargas completas de colecciones, eliminar bypasses de autorización y mantener la IA fuera del camino crítico.

## Cambios implementados

### Seguridad
- `TUC-SEC-001` — Eliminado el email hardcodeado como administrador. El backend usa exclusivamente el custom claim de Firebase `admin == true`.
- `TUC-SEC-002` — Firebase Admin ahora falla cerrado en producción si faltan `FIREBASE_PROJECT_ID`, `FIREBASE_CLIENT_EMAIL` o `FIREBASE_PRIVATE_KEY`.
- `TUC-SEC-003` — Firestore usa custom claim para administración y endurece escrituras de usuarios, favoritos y reportes mediante listas de campos permitidos e invariantes.
- `TUC-SEC-004` — Endurecido el allowlist de URLs externas: solo HTTPS y hosts exactos de YouTube, Instagram y TikTok; se rechazan rangos IP privados/locales y se elimina la aceptación de subdominios arbitrarios.
- El conector de YouTube no sigue redirecciones durante el enriquecimiento oEmbed.
- TikTok ya no acepta una URL sin ID de video válida.
- El detalle público de un video solo devuelve documentos `PUBLISHED`.

### Costos y rendimiento
- `TUC-DATA-001` — Catálogo público migrado de paginación por `page + slice()` a paginación por cursor de Firestore.
- El catálogo solicita solo `limit + 1` documentos para determinar `hasMore`.
- `total` se obtiene mediante `count()` cuando no hay búsqueda textual, evitando descargar toda la colección.
- `TUC-DATA-002` — Favoritos paginados por cursor y solo se consultan videos publicados correspondientes a la página solicitada.
- `TUC-PERF-001` — Eliminada la carga completa de favoritos.
- `TUC-API-001` — Reportes administrativos paginados y filtrables por estado.
- `TUC-API-002` — Métricas administrativas migradas a consultas de agregación `count()` en lugar de descargar videos, reportes y usuarios completos.
- Conteo de vistas cambiado a incremento atómico para evitar pérdida de actualizaciones concurrentes.
- Eliminado el ordenamiento/paginación en memoria de los listados administrativos.

## Decisiones de costo

1. No se incorpora un motor de búsqueda externo todavía. La búsqueda textual actual queda acotada a una ventana máxima de 500 documentos para evitar lecturas sin límite.
2. Se mantiene Firestore como almacenamiento principal y se priorizan índices, límites y cursores.
3. `count()` se usa solo donde aporta valor administrativo o al catálogo; para datasets enormes se evaluarán contadores materializados.
4. No se agrega IA al flujo normal de usuario.
5. Los enriquecimientos externos son best-effort y tienen timeout corto.

## Pendientes antes de producción

- Ejecutar `npm run typecheck`, `npm run build` y pruebas reales contra Firebase Emulator/entorno de staging.
- Revisar todos los índices requeridos por las nuevas combinaciones de `where + orderBy`.
- Actualizar frontend para consumir `nextCursor` en lugar de depender de `page`.
- Añadir pruebas automatizadas de SSRF, autorización admin, reglas Firestore, favoritos y paginación.
- Revisar CORS, headers de seguridad, body limits y rate limits en el servidor completo.
- Revisar embeds del frontend y garantizar que nunca se renderice HTML arbitrario.
- Configurar el primer administrador mediante Firebase Custom Claims; no volver a introducir un email hardcodeado.
- Realizar prueba de costos con datos sintéticos antes de producción.

## Fuente de verdad

Este documento registra el estado de la auditoría y debe actualizarse en cada nueva ronda de cambios relevantes.
